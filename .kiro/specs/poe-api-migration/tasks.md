# Implementation Plan

- [x] 1. Update dependencies and configuration
  - [x] 1.1 Add fastapi-poe to pyproject.toml dependencies
    - Add `fastapi-poe>=0.0.79` to the dependencies list
    - Run `uv sync` to install the new dependency
    - _Requirements: 1.1, 1.3_
  - [x] 1.2 Update backend/config.py for Poe.com
    - Replace `OPENROUTER_API_KEY` with `POE_API_KEY` environment variable
    - Update `COUNCIL_MODELS` to use Poe display names (e.g., "GPT-5", "Claude-Sonnet-4.5")
    - Update `CHAIRMAN_MODEL` to use Poe display name
    - Add `TITLE_MODEL` for fast title generation (e.g., "GPT-4o-Mini")
    - Remove `OPENROUTER_API_URL` constant
    - Add comments documenting available Poe bot names
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 2.4_
  - [ ]* 1.3 Write property test for API key security
    - **Property 1: API Key Security**
    - **Validates: Requirements 1.4**
    - Test that API key never appears in error messages or logs

- [x] 2. Implement Poe API client module
  - [x] 2.1 Create backend/poe.py with query_model function
    - Import fastapi_poe as fp
    - Implement `query_model(bot_name, messages, api_key)` async function
    - Use `fp.get_bot_response()` to query the bot
    - Accumulate PartialResponse chunks into complete response
    - Return dict with 'content' key for compatibility
    - Handle exceptions and return None on failure
    - _Requirements: 3.1, 3.3, 4.1, 4.2, 4.3_
  - [ ]* 2.2 Write property test for response accumulation
    - **Property 2: Response Accumulation**
    - **Validates: Requirements 3.3, 4.2, 4.3**
    - Test that PartialResponse chunks are correctly accumulated
  - [x] 2.3 Implement query_models_parallel function
    - Use asyncio.gather to query multiple bots concurrently
    - Return dict mapping bot_name to response (or None if failed)
    - Log errors for failed queries but continue with successful ones
    - _Requirements: 3.2, 3.4, 3.5_
  - [ ]* 2.4 Write property test for graceful degradation
    - **Property 3: Graceful Degradation**
    - **Validates: Requirements 3.4**
    - Test that partial failures return successful responses
  - [x] 2.5 Implement error handling for Poe-specific errors
    - Handle authentication errors (invalid API key)
    - Handle bot not found errors
    - Handle rate limit errors
    - Handle connection/timeout errors
    - _Requirements: 10.1, 10.2, 10.3, 10.4_

- [x] 3. Checkpoint - Verify Poe client works
  - Ensure all tests pass, ask the user if questions arise.

- [x] 4. Update council module
  - [x] 4.1 Update imports in backend/council.py
    - Change `from .openrouter import` to `from .poe import`
    - Update any references to use new function signatures
    - _Requirements: 3.1, 5.2, 6.1_
  - [x] 4.2 Update title generation to use Poe
    - Update `generate_conversation_title()` to use TITLE_MODEL from config
    - Use `query_model()` with the fast title model
    - _Requirements: 8.1, 8.2, 8.3_
  - [ ]* 4.3 Write property test for anonymization
    - **Property 4: Response Anonymization**
    - **Validates: Requirements 5.1, 5.4**
    - Test that responses are correctly labeled A, B, C, etc.
    - Test that label-to-model mapping is correct
  - [ ]* 4.4 Write property test for ranking parser
    - **Property 5: Ranking Parser Round-Trip**
    - **Validates: Requirements 5.3**
    - Test parsing of various ranking text formats

- [x] 5. Checkpoint - Verify council integration
  - Ensure all tests pass, ask the user if questions arise.

- [x] 6. Update documentation and cleanup
  - [x] 6.1 Update README.md
    - Change setup instructions from OPENROUTER_API_KEY to POE_API_KEY
    - Update model configuration examples to use Poe display names
    - Add link to poe.com/api_key for obtaining API key
    - _Requirements: 1.2_
  - [x] 6.2 Update CLAUDE.md technical notes
    - Document the migration from OpenRouter to Poe
    - Update architecture section with new poe.py module
    - Update model configuration notes
    - _Requirements: 2.4_
  - [x] 6.3 Remove backend/openrouter.py
    - Delete the old OpenRouter client module
    - Verify no remaining imports reference it
    - _Requirements: N/A (cleanup)_

- [x] 7. Verify API compatibility
  - [ ]* 7.1 Write property test for response format compatibility
    - **Property 7: API Response Format Compatibility**
    - **Validates: Requirements 9.2**
    - Test that JSON response structure matches expected format
  - [ ]* 7.2 Write property test for SSE event format
    - **Property 8: SSE Event Format Compatibility**
    - **Validates: Requirements 9.4**
    - Test that streaming events use correct event types
  - [ ]* 7.3 Write property test for request independence
    - **Property 6: Request Independence**
    - **Validates: Requirements 7.2**
    - Test that concurrent requests don't share state

- [x] 8. Final Checkpoint - Make sure all tests are passing
  - Ensure all tests pass, ask the user if questions arise.
