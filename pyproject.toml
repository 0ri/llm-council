[project]
name = "llm-council"
version = "0.1.0"
description = "Your LLM Council"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv~=1.0.0",
    "httpx~=0.27.0",
    "pydantic~=2.9.0",
    "fastapi-poe>=0.0.79",
    "boto3>=1.34",
    "tenacity>=8.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "ruff>=0.4.0",
]
